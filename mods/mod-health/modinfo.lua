name = "My Mod"
description = "Demo Mod: 将所有玩家的生命上限改为500"
author = "Your Name"
version = "1.0.0"

forumthread = ""

api_version = 10

-- Compatible with both the base game and Reign of Giants
dont_starve_compatible = false
reign_of_giants_compatible = false
dst_compatible = true

-- Icon and image files (optional - comment out if you don't have icon files)
-- icon_atlas = "modicon.xml"
-- icon = "modicon.tex"

-- Server only mod (set to true if this mod should only run on the server)
server_only_mod = false

-- Client only mod (set to true if this mod should only run on the client)
client_only_mod = false

-- All clients need this mod (set to false if mod can work with clients not having it)
all_clients_require_mod = true

-- Priority (higher number = loads first, use negative for late loading)
priority = 0

-- Configuration options (optional)
-- configuration_options = {}

